[package]
name = "mathgen"
version = "1.0.0"
edition = "2021"
description = "Generate printable PDF worksheets of stacked double-digit math problems for young kids"
license = "MIT"

[dependencies]

[profile.release]
opt-level = "z"
lto = true
strip = true
panic = "abort"
